const keyCodes = require('../../../../static/key-codes/key-event-codes');

// The application key event enums
const enums = Object.freeze({
  KEYA: keyCodes.KEYA,
  KEYD: keyCodes.KEYD,
  KEYW: keyCodes.KEYW,
  KEYS: keyCodes.KEYS
});

module.exports = enums;
